<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>📒LedgerLite - Edit Entry</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.2.0/remixicon.min.css"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              softblue: {
                50: "#F0F7FF",
                100: "#E0EFFE",
                200: "#BAE0FD",
                300: "#8CCBFA",
                400: "#5AB1F7",
                500: "#2F95F0",
                600: "#1578D3",
                700: "#0C5CAA",
                800: "#094580",
                900: "#063159",
              },
              zinc: {
                100: "#F4F4F4",
                200: "#E0E0E0",
              },
              blue: {
                500: "#2F95F0",
                600: "#1578D3",
              },
            },
          },
        },
      };

      document.addEventListener("DOMContentLoaded", function () {
        const form = document.querySelector("form");
        form.addEventListener("keypress", function (event) {
          if (event.key === "Enter") {
            event.preventDefault(); // Prevent default Enter key behavior
            form.submit(); // Explicitly submit the form
          }
        });
      });
    </script>
</head>
<body class="bg-softblue-50 font-sans">
    <nav class="bg-white shadow-sm">
        <div class="container mx-auto px-6 py-4 flex justify-between items-center">
            <h1 class="text-2xl font-semibold text-softblue-700">📒LedgerLite</h1>
            <div class="navlinks flex gap-6">
                <a class="text-softblue-600 hover:text-softblue-800 transition-colors duration-200" href="/">Dashboard</a>
                <a class="text-softblue-600 hover:text-softblue-800 transition-colors duration-200" href="/create">New Entry</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto px-6 py-8">
        <h2 class="text-3xl font-bold mb-8 text-softblue-800 text-center">Edit Entry</h2>
        <div class="flex justify-center">
            <form action="/edithisaab" method="post" class="space-y-4 w-full max-w-lg">
                <input
                    name="title"
                    class="text-md px-3 py-2 w-full mb-5 rounded-md bg-zinc-200 border border-softblue-200 focus:outline-none focus:ring-2 focus:ring-softblue-500"
                    type="text"
                    placeholder="Title of the record"
                    value="Existing Title" <!-- Prepopulate with the existing title -->
                    required
                />
                <!-- <textarea
                    name="content"
                    class="block w-full px-3 py-2 resize-none bg-zinc-200 rounded-md min-h-96 border border-softblue-200 focus:outline-none focus:ring-2 focus:ring-softblue-500"
                    placeholder="Detailed description of the record"
                    required
                >Existing content goes here</textarea>
                <input
                    type="submit"
                    class="block w-full px-5 py-3 bg-softblue-500 text-white rounded-md hover:bg-softblue-600 transition-colors duration-200 cursor-pointer"
                    value="Update Entry"
                />
            </form>
        </div>
    </div>
</body> -->
<!-- </html> --> -->
